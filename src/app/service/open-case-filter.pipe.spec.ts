import { OpenCaseFilterPipe } from './open-case-filter.pipe';

describe('OpenCaseFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new OpenCaseFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
