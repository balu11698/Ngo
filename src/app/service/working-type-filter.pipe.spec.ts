import { WorkingTypeFilterPipe } from './working-type-filter.pipe';

describe('WorkingTypeFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new WorkingTypeFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
