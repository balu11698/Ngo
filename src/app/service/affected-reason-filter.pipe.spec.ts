import { AffectedReasonFilterPipe } from './affected-reason-filter.pipe';

describe('AffectedReasonFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new AffectedReasonFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
