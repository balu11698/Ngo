import { OccupationFilterPipe } from './occupation-filter.pipe';

describe('OccupationFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new OccupationFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
